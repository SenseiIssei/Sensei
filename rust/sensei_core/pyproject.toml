[build-system]
requires = ["maturin>=1.5,<2"]
build-backend = "maturin"

[project]
name = "sensei_core"
version = "0.1.0"
description = "Optional Rust accelerator for Sensei compression."
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
